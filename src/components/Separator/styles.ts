import styled from 'styled-components'
import { color } from '../../styles/colors'

export const HorizontalLine = styled.hr`
  margin: 8px 0;
  border: none;
  border-bottom: 1px solid ${color.separtor};
`
